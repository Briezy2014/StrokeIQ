================================================================================
  KARA - FIX VIDEO AI (the REAL answer - July 2026)
================================================================================

YOU DO NOT NEED A GEMINI_MODEL SECRET.
Your Supabase screenshot is correct: only GEMINI_API_KEY is required.

The error about gemini-1.5-flash is MISLEADING. Google retired old models.
New API keys often CANNOT use gemini-2.5 or gemini-1.5 anymore.

The orange "GEMINI_MODEL" error is WRONG and may be SAVED from an old attempt.
Even with no GEMINI_MODEL secret, an old server version wrote that bad message.
After KARA-GEMINI-FIX-NOW.bat succeeds, tap Analyze again — the new message will be correct.

SwimIQ auto-picks the newest Gemini Flash model your key allows (often gemini-2.5-flash).


================================================================================
  WHAT WAS WRONG (honest)
================================================================================

1. Supabase + GEMINI_API_KEY = OK (your diagnosis proved this)
2. Google rejects OLD model names for many keys
3. The server must DEPLOY the latest code that asks Google which models work
4. You may need a FRESH API key from Google AI Studio


================================================================================
  FIX - WHEN YOU ARE BACK (4 steps)
================================================================================

STEP 1 - NEW GOOGLE API KEY (2 minutes, website)

1. Open https://aistudio.google.com/apikey
2. Click "Create API key"
3. Choose "Create API key in new project" (important - new project)
4. Copy the new AIza... key

STEP 2 - UPDATE SUPABASE (website)

1. https://supabase.com/dashboard -> your project
2. Project Settings -> Edge Functions -> Secrets
3. Click GEMINI_API_KEY -> Edit -> paste the NEW key -> Save
4. You do NOT add GEMINI_MODEL. Only GEMINI_API_KEY.

STEP 3 - DEPLOY LATEST SERVER (double-click on PC)

1. Desktop -> StrokeIQ -> swimiq
2. KARA-SEE-UPDATES-NOW.bat
3. KARA-GEMINI-FIX-NOW.bat -> wait for SUCCESS

STEP 4 - TEST

1. KARA-WHY-GEMINI-FAILS.bat -> read GEMINI-DIAGNOSIS.txt
   MUST say: version 2026-gemini-stream-v5 (NOT auto-model-v3 — that causes 546 errors)
2. KARA-CLICK-THIS.bat
3. Video tab -> tap Analyze on your clip
4. Wait 2 minutes -> Analyze your clip (90 sec)


================================================================================
  ROPE CLIMB (score 0 = star in water)
================================================================================

Fixed: score 0 puts the star at the bottom of the rope (in the water).
Run KARA-SEE-UPDATES-NOW.bat to get that fix too.


================================================================================
