SwimIQ — what to do (Desktop\StrokeIQ)

FOR ANALYSIS RIGHT NOW (one path only):
  1) Double-click GET-LATEST-FIXED-APP.bat
  2) Double-click FINAL-TRY-THIS-ONLY.bat
  3) Follow FINAL-TRY-THIS-ONLY.txt

Do NOT use swimiqapp.com for Elite analysis on this try.
Use the Chrome window that FINAL-TRY-THIS-ONLY.bat opens
(address bar must say localhost or 127.0.0.1).

If upload fails:
  OPEN-UPLOAD-SQL.bat + FIX-VIDEO-UPLOAD-NOW.txt

If storage/health fails:
  FIX-STORAGE.bat, then FINAL-TRY-THIS-ONLY.bat again

If FFmpeg missing:
  RESTART-ELITE-AFTER-FFMPEG.bat, then FINAL-TRY-THIS-ONLY.bat again
