============================================================
  GOOD MORNING - Elite analysis (ONE path)
============================================================

Only do this:

  1. Double-click   START-SWIMIQ-WITH-ELITE.bat
  2. Leave the Elite black window OPEN
  3. Sign in on localhost (127.0.0.1) - NOT swimiqapp.com
  4. Elite tab -> Run Elite Analysis on a short clip

If it stops with a red [FAIL] message, that message is the real reason.
Do NOT hunt other .bat files. Read the Elite window + elite-start.log.

Known one-time PC setup (only if the starter says so):
  - Python 3.11 installed
  - FFmpeg installed:  winget install Gyan.FFmpeg
  - ONE GEMINI_API_KEY= line in swimiq\.env (optional for coaching upgrade)

Do NOT click FIX-*.bat files. They all redirect here anyway.
============================================================
